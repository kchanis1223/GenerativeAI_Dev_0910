# Server-side only. Never use VITE_* for these keys.
OPENAI_API_KEY=
TMAP_APP_KEY=
USE_MOCK=1
# Confirm model availability before integration (design section 2.3).
MAIN_MODEL=
CLASSIFIER_MODEL=
# Proposed integration defaults; consumed when B-17 is implemented.
TMS_POLL_INTERVAL_SECONDS=1
TMS_POLL_MAX_ATTEMPTS=30
TOOL_MAX_RETRIES=3
MAX_TOOL_ITERATIONS=6
