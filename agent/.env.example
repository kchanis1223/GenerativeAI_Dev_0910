# Server-side only. Never use VITE_* for these keys.
OPENAI_API_KEY=
TMAP_APP_KEY=
USE_MOCK=1
# B-07 temporary model selection. Confirm account availability before live calls.
MAIN_MODEL=gpt-5.4-mini
CLASSIFIER_MODEL=gpt-5.4-mini
# Proposed integration defaults; consumed when B-17 is implemented.
TMS_POLL_INTERVAL_SECONDS=1
TMS_POLL_MAX_ATTEMPTS=30
TOOL_MAX_RETRIES=3
MAX_TOOL_ITERATIONS=6
